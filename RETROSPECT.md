# HuntTimer — 결과 및 배운 점

---

## 결과

- 홈 · 타이머 · 기록 · 프로필 4개 핵심 탭을 포함한 **7개 화면** 단독 완성
- **10개 버그** 발견 및 해결 (크래시 4건 포함)
- 지인 사용자 피드백 수집 후 **3개 기능 개선** (장난감 멀티선택, 전체 기록 목록, 놀이시간 차트)
- Realm · RxSwift · CALayer 등 **5개 외부 라이브러리** 실제 프로덕션 수준으로 적용

---

## 배운 점

### 1. iOS 플랫폼 이해

실기기에서만 재현되는 dyld 크래시를 겪으면서, Xcode가 자동 삽입하는 빌드 플래그가 구형 OS에서 심볼 충돌을 일으킬 수 있다는 것을 알게 됐습니다. 이후 빌드 설정을 직접 확인하는 습관이 생겼습니다.

iOS 앱 샌드박스 구조를 통해 컨테이너 UUID가 재빌드마다 변경된다는 사실도 직접 경험했습니다. 배너 이미지 경로 소실 버그를 해결하면서, 절대 경로 대신 파일명만 저장하고 런타임에 재조합하는 패턴을 적용하게 됐습니다.

### 2. UIKit 렌더링 파이프라인

`layoutSubviews()`가 단순히 레이아웃을 갱신하는 메서드가 아니라, 조건에 따라 반복 호출되는 사이클임을 `EXC_BAD_ACCESS` 크래시를 통해 직접 확인했습니다. CALayer와 UIView 계층을 분리해서 설계해야 한다는 원칙을 체득했습니다.

SnapKit의 `makeConstraints`가 뷰 계층 완성 이후에 호출되어야 한다는 Auto Layout의 기본 전제도, 런타임 크래시를 디버깅하는 과정에서 명확히 이해하게 됐습니다.

### 3. 데이터 레이어 (Realm)

Realm 관리 객체는 `delete()` 직후 invalidated 상태가 되므로, 필요한 값을 삭제 전에 값 타입으로 반드시 복사해야 한다는 생명주기 규칙을 고양이 삭제 크래시를 통해 체득했습니다.

`LinkingObjects` 역방향 탐색보다 직접 `filter("session == %@")` 쿼리가 더 안정적임을 실험으로 확인하고, 이후 Realm 쿼리를 작성할 때 신뢰성을 우선 고려하게 됐습니다.

### 4. UX 관점 사고

실제 사용자 피드백을 수집하면서, 개발자가 당연하다고 생각한 것과 사용자가 실제로 불편함을 느끼는 지점 사이의 간극을 경험했습니다. 예를 들어 타이머가 끝나도 계속 기록되는 오버타임 기능은 이미 구현되어 있었지만, 전체 기록 목록 조회나 장난감 멀티선택처럼 사용 흐름에서 자연스럽게 기대하는 기능은 놓치고 있었습니다. 사용자의 실제 행동 흐름을 먼저 고민하고 기능을 설계하는 것이 중요하다는 것을 배웠습니다.

### 5. 설계와 구조

MVVM + Input-Output 패턴을 처음 적용하면서, View와 ViewModel의 책임을 명확히 분리하는 것이 기능 추가와 버그 수정 시 얼마나 유리한지 직접 체감했습니다. 기능이 늘어날수록 "지금 당장 동작하는 코드"와 "나중에 고치기 쉬운 코드" 사이의 균형을 고민하게 됐고, 초기 구조 설계에 더 많은 시간을 투자해야 한다는 것을 배웠습니다.
